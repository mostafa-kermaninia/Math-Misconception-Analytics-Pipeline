# Project_P2_Std_numbers

## Project Structure:
- `database/`: Contains the raw data files and database.
- `scripts/`: Contains Python scripts for loading data, preprocessing, and importing to the database.
- `pipeline.py`: Orchestrates the entire process of importing data into the database.

## How to Run the Pipeline:
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
